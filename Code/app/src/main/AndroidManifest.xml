<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/vvit_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/vvit_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoApp">
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/Theme.DemoApp.NoActionBar"
            android:exported="false"> </activity>

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.DemoApp.NoActionBar"
            android:exported="true"> </activity>

        <activity android:name=".MainActivity5"> </activity>

        <activity
            android:name=".MainActivity4"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"> </activity>

        <activity android:name=".MainActivity3"> </activity>

        <activity
            android:name=".MainActivity2"
            android:theme="@style/Theme.DemoApp.NoActionBar"> </activity>

        <activity
            android:name=".SignInActivity"
            android:theme="@style/Theme.DemoApp.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>